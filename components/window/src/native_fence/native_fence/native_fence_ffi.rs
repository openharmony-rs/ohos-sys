// automatically generated by rust-bindgen 0.71.1

#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]

extern "C" {
    /// Checks if the fenceFd is valid.
    ///
    /// # Arguments
    ///
    /// * `fenceFd` - Indicates a file descriptor handle, which is used for timing synchronization.
    ///
    /// # Returns
    ///
    /// * Returns true if the fenceFd is valid.
    /// Returns false if the fenceFd is a negative integer.
    ///
    /// Available since API-level: 20
    ///
    /// Version: 1.0
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_NativeFence_IsValid(fenceFd: ::core::ffi::c_int) -> bool;
    /// Waits for a fence signal. The maximum waiting time is determined by the timeout parameter.
    /// The incoming fenceFd needs to be closed by the user themselves.
    ///
    /// # Arguments
    ///
    /// * `fenceFd` - Indicates a file descriptor handle, which is used for timing synchronization.
    ///
    /// * `timeout` - Indicates the timeout duration.
    /// The unit is milliseconds, 0 represents immediate return.
    ///
    /// # Returns
    ///
    /// * Returns true if the fence signaled.
    /// Returns false in the following cases:
    /// 1.if the fenceFd is a negative integer.
    /// 2.no event occurred within the specified timeout period.
    /// 3.the underlying poll interface call failed.
    /// 4.the timeout value is 0.
    /// 5.failed to duplicate the file descriptor.
    ///
    /// Available since API-level: 20
    ///
    /// Version: 1.0
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_NativeFence_Wait(fenceFd: ::core::ffi::c_int, timeout: u32) -> bool;
    /// Waits forever for a fence signal.
    /// The incoming fenceFd needs to be closed by the user themselves.
    ///
    /// # Arguments
    ///
    /// * `fenceFd` - Indicates a file descriptor handle, which is used for timing synchronization.
    ///
    /// # Returns
    ///
    /// * Returns true if the fence signaled.
    /// Returns false in the following cases:
    /// 1.if the fenceFd is a negative integer.
    /// 2.no incidents have occurred, permanent waiting.
    /// 3.failed to duplicate the file descriptor.
    ///
    /// Available since API-level: 20
    ///
    /// Version: 1.0
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_NativeFence_WaitForever(fenceFd: ::core::ffi::c_int) -> bool;
    /// Close the fenceFd.
    ///
    /// # Arguments
    ///
    /// * `fenceFd` - Indicates a file descriptor handle, which is used for timing synchronization.
    /// This value is a non negative integer.
    ///
    /// Available since API-level: 20
    ///
    /// Version: 1.0
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_NativeFence_Close(fenceFd: ::core::ffi::c_int);
}

# ohos-netmanager-sys

Raw Rust FFI bindings to the OpenHarmony NetworkKit (`netmanager`) native APIs.

The C headers are under `network/netmanager/` in the OpenHarmony SDK.

## License

Licensed under Apache-2.0, matching the OpenHarmony SDK.
